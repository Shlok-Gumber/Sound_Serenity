<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.intro_video"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add this line to request Internet permission -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Intro_Video"
        tools:targetApi="31">

        <activity
            android:name="com.example.intro_video.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.intro_video.SignUpActivity"
            android:exported="true" />

        <service
            android:name="com.example.intro_video.VideoPlaybackService"
            android:exported="false" />
    </application>

</manifest>
